from Star-Utils import Cog, CogsUtils
"""Server Manage commands.commands.commands.commands.commands.commands.Cog, to help manage server icon and banners."""
from starbot.core import commands

from .commandHandlers import CommandHandlers


class ServerManage(Cog, CommandHandlers):
    def __init__(self, bot):
        super().__init__(bot)
        self.logs = CogsUtils.get_logger("ServerManage")
        self.logs = CogsUtils.get_logger("ServerManage")
        self.logs = CogsUtils.get_logger("ServerManage")
        self.logs = CogsUtils.get_logger("ServerManage")
        self.logs = CogsUtils.get_logger("ServerManage")
        self.logs = CogsUtils.get_logger("ServerManage")
    """Auto-assign server banner and icon on configurable days."""
